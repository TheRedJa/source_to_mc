# Third-party licences

src2mc is built from Rust crates that are compiled into its executable, so
their terms travel with any copy of it. This file is generated by
`cargo about generate` from `Cargo.lock`; see `about.toml` for how.

Nothing here is copyleft, and none of it restricts what src2mc itself is
licensed under — see LICENSE and NOTICE for that.

{{#each overview}}
- {{name}} ({{count}} {{#if (eq count 1)}}crate{{else}}crates{{/if}})
{{/each}}

{{#each licenses}}
## {{name}}

Used by:

{{#each used_by}}
- [{{crate.name}} {{crate.version}}]({{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}})
{{/each}}

```
{{text}}
```

{{/each}}
